import PageTitleBar from "../layout/page-title-bar/PageTitleBar";

export default function Inspections() {

    return (
        <>
            <PageTitleBar pageTitle="Inspections" /> {/* should be in Layout, but we are saving time here */}
        </>
    );
}
